# 🔧 Руководство по восстановлению файлов server/

## ⚠️ Ситуация

После выполнения `git reset --hard` в папке `server/` были удалены:
- ❌ `src/` - все исходники TypeScript
- ❌ `prisma/` - схема БД и миграции  
- ❌ `package.json` - зависимости
- ❌ `tsconfig.json` - конфигурация TypeScript
- ❌ Другие конфигурационные файлы

## ✅ Что осталось

- ✓ `dist/` - скомпилированные JavaScript файлы
- ✓ `dist/*.map` - source maps (частично могут помочь)
- ✓ `node_modules/` - зависимости установлены
- ✓ `uploads/` - пользовательские файлы

## 🔄 Варианты восстановления

### Вариант 1: Восстановление из remote репозитория (РЕКОМЕНДУЕТСЯ)

Если у вас есть remote репозиторий для backend:

```bash
cd /Users/balabiturembek/Desktop/hub/server

# Проверить remote
git remote -v

# Если есть remote, восстановить из него
git fetch origin
git reset --hard origin/main
# или
git reset --hard origin/master
```

### Вариант 2: Восстановление из истории Git

```bash
cd /Users/balabiturembek/Desktop/hub/server

# Посмотреть все коммиты (даже удаленные)
git reflog

# Найти последний коммит перед reset
# Например, если это HEAD@{1}:
git reset --hard HEAD@{1}

# Или если знаете хеш коммита:
git reset --hard <commit-hash>
```

### Вариант 3: Восстановление из родительского репозитория

Если файлы были в родительском репозитории `hub`:

```bash
cd /Users/balabiturembek/Desktop/hub

# Проверить есть ли коммиты с файлами server
git log --all --oneline -- server/src/

# Если есть, восстановить из последнего коммита
git checkout HEAD -- server/src/
git checkout HEAD -- server/prisma/
git checkout HEAD -- server/package.json
git checkout HEAD -- server/tsconfig.json
```

### Вариант 4: Восстановление из backup/IDE истории

- **VS Code**: Проверить Local History extension
- **Cursor**: Проверить историю файлов в IDE
- **Time Machine** (macOS): Если включен, восстановить из бэкапа
- **GitHub Desktop**: Проверить локальную историю

### Вариант 5: Восстановление из source maps (частичное)

Source maps содержат информацию о структуре, но не полный код. Можно попробовать:

```bash
# Использовать инструменты для восстановления
npm install -g source-map-explorer
# Но это сложно и не всегда работает
```

### Вариант 6: Пересоздание структуры (если ничего не помогло)

Если ничего не помогло, можно пересоздать базовую структуру и использовать скомпилированные файлы как референс.

## 🚨 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ

1. **НЕ закрывать терминал** - может быть полезная информация
2. **НЕ делать больше git команд** пока не восстановим
3. **Проверить remote репозиторий** - это самый надежный способ
4. **Проверить reflog** - может быть там есть коммиты

## 📋 Чеклист восстановления

- [ ] Проверить `git remote -v` в папке server
- [ ] Проверить `git reflog` для поиска удаленных коммитов
- [ ] Проверить родительский репозиторий hub на наличие файлов
- [ ] Проверить IDE историю (VS Code, Cursor)
- [ ] Проверить Time Machine (macOS)
- [ ] Проверить есть ли backup где-то еще

## 💡 После восстановления

1. **Настроить правильную структуру репозиториев:**
   - Убедиться что `server/` - это отдельный git репозиторий
   - Или настроить как git submodule в родительском репозитории

2. **Создать remote репозиторий:**
   ```bash
   cd server
   git remote add origin <your-backend-repo-url>
   git push -u origin main
   ```

3. **Добавить .gitignore** (уже добавлен)

4. **Создать .env.example** для документации

