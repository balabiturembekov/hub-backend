# ✅ Production Readiness Checklist

## 🔒 Безопасность

### Критично - Требует исправления перед продакшеном:

- [ ] **JWT_SECRET** - Заменить дефолтное значение в `docker-compose.yml`
  - Текущее: `your-super-secret-jwt-key-change-in-production`
  - Действие: Использовать сильный случайный секрет (минимум 32 символа)
  - Где: `docker-compose.yml:51`

- [ ] **Пароли БД** - Заменить дефолтные пароли
  - Текущее: `hubstaff_password`
  - Действие: Использовать сильные пароли из переменных окружения
  - Где: `docker-compose.yml:9, 50`

- [ ] **Переменные окружения** - Вынести секреты в `.env` файлы
  - Создать `.env` файлы для каждого сервиса
  - Использовать Docker secrets или внешние системы управления секретами
  - НЕ коммитить `.env` файлы в git

- [ ] **URLs** - Заменить localhost на реальные домены
  - `NEXT_PUBLIC_API_URL` - должен указывать на продакшен API
  - `NEXT_PUBLIC_WS_URL` - должен указывать на продакшен WebSocket
  - `FRONTEND_URL` - должен указывать на продакшен frontend
  - Где: `docker-compose.yml:56, 86-88`

## 📊 Мониторинг и Логирование

- [x] **Sentry** - Настроен и работает
  - DSN настроен
  - Source maps отключены в браузере (безопасность)
  - Sample rates настроены для продакшена (10%)

- [ ] **Console.log** - Проверить и убрать лишние логи
  - Текущее: 217 console.log/warn/debug в коде
  - Рекомендация: Использовать условное логирование только в development
  - Или использовать структурированное логирование (Pino на backend)

## 🐳 Docker и Развертывание

- [x] **Docker Compose** - Настроен
  - Health checks настроены
  - Restart policies настроены
  - Volumes настроены

- [ ] **Production Dockerfile** - Проверить оптимизацию
  - Multi-stage builds для уменьшения размера
  - Минимальные базовые образы
  - Правильная установка зависимостей

- [ ] **Environment Variables** - Все переменные документированы
  - Создать `.env.example` файлы
  - Документировать все обязательные переменные

## 🔧 Конфигурация

- [x] **Next.js Config** - Оптимизирован для продакшена
  - Source maps отключены в браузере
  - Standalone output для Docker
  - Sentry интегрирован

- [x] **CORS** - Настроен правильно
  - В продакшене ограничен только FRONTEND_URL
  - В development разрешены localhost URLs

- [x] **Security Headers** - Helmet настроен
  - CSP отключен (должен быть на frontend)
  - Другие заголовки безопасности активны

## 📝 Документация

- [x] **README** - Есть базовая документация
- [x] **DOCKER.md** - Есть инструкции по Docker
- [x] **SENTRY.md** - Есть документация по Sentry

## ⚠️ Перед запуском в продакшен:

1. **Обязательно:**
   - [ ] Изменить все пароли и секреты
   - [ ] Настроить правильные URLs
   - [ ] Настроить SSL/TLS сертификаты
   - [ ] Настроить резервное копирование БД
   - [ ] Настроить мониторинг (Sentry уже настроен)

2. **Рекомендуется:**
   - [ ] Настроить rate limiting
   - [ ] Настроить CDN для статических файлов
   - [ ] Настроить автоматические бэкапы
   - [ ] Настроить алерты в Sentry
   - [ ] Провести нагрузочное тестирование

3. **Опционально:**
   - [ ] Уменьшить количество console.log
   - [ ] Настроить структурированное логирование
   - [ ] Настроить метрики (Prometheus/Grafana)

## 🚀 Команды для продакшена:

```bash
# 1. Создать .env файлы с реальными секретами
# 2. Обновить docker-compose.yml с правильными URLs
# 3. Запустить:
docker-compose -f docker-compose.yml up -d --build

# 4. Проверить логи:
docker-compose logs -f

# 5. Проверить health checks:
docker-compose ps
```

## 📌 Важные замечания:

- **НЕ используйте** дефолтные пароли в продакшене
- **НЕ коммитьте** `.env` файлы с реальными секретами
- **Используйте** переменные окружения для всех секретов
- **Проверьте** все URLs перед деплоем
- **Настройте** мониторинг и алерты

