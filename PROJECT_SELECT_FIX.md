# 🔧 Исправление отображения Select проекта

## Проблема

У сотрудников (employees) не отображалось поле Select для выбора проекта в карточке трекера. У админов и owner отображалось "No project".

## Причина

На странице `/tracking` (`app/tracking/page.tsx`) не вызывался `loadProjects()`, поэтому проекты не загружались в store. В результате:

1. **Для сотрудников:** Условие `(!projects || !Array.isArray(projects) || projects.filter((p) => p.status === 'active').length === 0)` было `true`, и показывалось сообщение "No Active Projects" вместо Select.

2. **Для админов/owner:** Select отображался, но так как `projects` был пустым массивом, в нем была только опция "No project".

## Решение

Добавлен вызов `loadProjects()` в `app/tracking/page.tsx` во всех местах, где загружаются данные:

1. Когда пользователь уже загружен в store (строка 90-94)
2. Когда пользователь загружается из storage (строка 117-121)
3. После успешной инициализации auth (строка 130-134)

Также добавлена проверка `projects && Array.isArray(projects)` в компоненте `TimeTracker.tsx` для безопасности.

## Файлы изменены

- `app/tracking/page.tsx` - добавлен `loadProjects()` в загрузку данных
- `components/TimeTracker.tsx` - улучшена проверка наличия проектов

## Результат

Теперь:
- ✅ У сотрудников отображается Select с активными проектами
- ✅ У админов/owner отображается Select с опцией "No project" и активными проектами
- ✅ Если у сотрудника нет активных проектов, показывается предупреждение (как и должно быть)

