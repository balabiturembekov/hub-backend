# 🔒 Исправление валидации проекта

## Проблема

Сотрудники (employees) могли обойти фронтенд валидацию и создать time entry без проекта через прямой API вызов.

## Анализ

### ✅ Фронтенд валидация (работает корректно)
1. **В `handleStart`** (строки 281-290): проверка для employees
2. **В keyboard shortcuts** (строки 526-533): проверка для employees  
3. **В UI** (строки 747-748): кнопка disabled для employees без проекта

### ❌ Бэкенд валидация (отсутствовала)
- В `CreateTimeEntryDto` `projectId` помечен как `@IsOptional()`
- В `time-entries.service.ts` не было проверки, что для employees `projectId` обязателен
- Сотрудник мог обойти фронтенд и создать time entry без проекта через API

## Решение

Добавлена валидация на бэкенде в `time-entries.service.ts`:

```typescript
// CRITICAL: For employees, project is required
if (user.role === UserRole.EMPLOYEE && !dto.projectId) {
  throw new BadRequestException('Project is required for employees. Please select a project before starting the timer.');
}
```

## Результат

Теперь валидация работает на двух уровнях:
1. **Фронтенд** - предотвращает попытки запустить таймер без проекта (UX)
2. **Бэкенд** - гарантирует, что сотрудники не могут обойти валидацию через API (безопасность)

## Дополнительная защита

Также добавлена проверка в методе `update`:
- Сотрудники не могут удалить проект из существующего time entry (установить `projectId` в `null`)
- Это предотвращает обход валидации через обновление существующей записи

## Файлы изменены

- `server/src/time-entries/time-entries.service.ts` - добавлена проверка роли пользователя в методах `create` и `update`

## Тестирование

После деплоя проверьте:
1. ✅ Сотрудник не может запустить таймер без проекта через UI
2. ✅ Сотрудник не может создать time entry без проекта через API (должна быть ошибка 400)
3. ✅ Админы/владельцы могут создавать time entry без проекта (как и раньше)

