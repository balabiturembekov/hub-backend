# План реализации отслеживания активности сотрудников

## 📋 Текущая ситуация

**Текущая архитектура:**

- Веб-приложение на Next.js (браузер)
- Уже реализованы скриншоты через `getDisplayMedia` API
- Отслеживание времени работы

## ⚠️ Ограничения браузера

### Что НЕВОЗМОЖНО в браузере:

1. ❌ Отслеживание движений мыши вне окна браузера
2. ❌ Отслеживание нажатий клавиатуры вне окна браузера
3. ❌ Получение списка открытых приложений системы
4. ❌ Отслеживание активности в других приложениях
5. ❌ Мониторинг системных ресурсов (CPU, память)

### Что ВОЗМОЖНО в браузере:

1. ✅ Отслеживание движений мыши внутри окна браузера
2. ✅ Отслеживание нажатий клавиатуры внутри окна браузера
3. ✅ Отслеживание фокуса окна (когда пользователь переключается на другое приложение)
4. ✅ Отслеживание времени активности в браузере
5. ✅ Скриншоты (уже реализовано)

## 🎯 Рекомендуемое решение

### Вариант 1: Десктоп клиент (РЕКОМЕНДУЕТСЯ)

**Для полного отслеживания нужен нативный десктоп клиент:**

#### Технологии:

- **Electron** (кроссплатформенный) - проще интегрировать с веб-приложением
- **Tauri** (более легкий, Rust-based)
- **Нативные приложения** (Windows: C#/.NET, macOS: Swift, Linux: C++/Qt)

#### Функционал десктоп клиента:

1. **Отслеживание мыши:**

   - Глобальные хуки на движения мыши
   - Подсчет кликов
   - Траектория движения

2. **Отслеживание клавиатуры:**

   - Глобальные хуки на нажатия клавиш
   - Подсчет нажатий в минуту
   - Отслеживание комбинаций клавиш

3. **Отслеживание приложений:**

   - Получение списка активных окон
   - Отслеживание переключений между приложениями
   - Время работы в каждом приложении
   - Скриншоты активных окон

4. **Дополнительно:**
   - Мониторинг системных ресурсов
   - Отслеживание интернет-активности
   - Логирование URL посещенных сайтов

#### Архитектура:

```
Desktop Client (Electron/Tauri)
    ↓
    API calls to Backend
    ↓
    Backend (NestJS) - уже существует
    ↓
    Database (PostgreSQL) - новая таблица ActivityLog
    ↓
    Web App (Next.js) - отображение данных
```

### Вариант 2: Браузерное отслеживание (ограниченное)

**Можно добавить базовое отслеживание активности в браузере:**

#### Реализация:

1. **Отслеживание активности в браузере:**

   - События мыши (`mousemove`, `click`)
   - События клавиатуры (`keydown`, `keypress`)
   - Отслеживание фокуса окна (`visibilitychange`, `blur`, `focus`)
   - Отслеживание времени бездействия

2. **Ограничения:**
   - Работает только когда вкладка активна
   - Не отслеживает активность вне браузера
   - Требует постоянного открытого окна

## 📊 Структура базы данных

### Новая таблица: `ActivityLog`

```prisma
model ActivityLog {
  id          String   @id @default(uuid())
  userId      String
  timeEntryId String?  // Привязка к записи времени
  timestamp   DateTime @default(now())

  // Типы активности
  type        ActivityType // MOUSE_MOVE, KEYBOARD_PRESS, APP_SWITCH, IDLE

  // Данные активности
  mouseClicks     Int?     // Количество кликов
  keyboardPresses Int?    // Количество нажатий
  mouseDistance   Float?   // Расстояние движения мыши в пикселях

  // Приложения (для десктоп клиента)
  activeApp      String?   // Название активного приложения
  appTitle       String?   // Заголовок окна
  appCategory    String?   // Категория приложения

  // Метаданные
  platform       String?   // Windows, macOS, Linux
  userAgent      String?   // Для браузерного отслеживания

  user           User      @relation(fields: [userId], references: [id])
  timeEntry      TimeEntry? @relation(fields: [timeEntryId], references: [id])

  @@index([userId, timestamp])
  @@index([timeEntryId])
  @@map("activity_logs")
}

enum ActivityType {
  MOUSE_MOVE
  MOUSE_CLICK
  KEYBOARD_PRESS
  APP_SWITCH
  IDLE
  SCREENSHOT
}
```

## 🚀 План реализации

### Этап 1: Браузерное отслеживание (быстро, ограниченно)

1. **Создать хук `useActivityTracking.ts`:**

   - Отслеживание событий мыши
   - Отслеживание событий клавиатуры
   - Отслеживание фокуса окна
   - Агрегация данных (клики/минуту, нажатия/минуту)

2. **Backend API:**

   - Endpoint для отправки данных активности
   - Сохранение в БД
   - Агрегация данных

3. **UI для отображения:**
   - Графики активности
   - Статистика по активности
   - Индикаторы активности в реальном времени

### Этап 2: Десктоп клиент (полное решение)

1. **Создать Electron приложение:**

   - Настройка проекта
   - Интеграция с существующим API
   - Глобальные хуки на мышь/клавиатуру

2. **Реализация отслеживания:**

   - Системные API для получения активных приложений
   - Глобальные хуки событий
   - Фоновая служба

3. **Интеграция с веб-приложением:**
   - Автоматическая авторизация
   - Синхронизация данных
   - Настройки отслеживания

## 🔒 Безопасность и приватность

### Важные моменты:

1. **Согласие пользователя:**

   - Четкое уведомление о отслеживании
   - Возможность отключения
   - Прозрачность данных

2. **Хранение данных:**

   - Шифрование чувствительных данных
   - Ограничение доступа
   - Политика хранения (удаление старых данных)

3. **Соответствие законам:**
   - GDPR (для EU)
   - Локальные законы о приватности
   - Уведомление сотрудников

## 📝 Рекомендации

**Для начала:**

1. Реализовать браузерное отслеживание (быстро, показывает базовую активность)
2. Добавить UI для отображения данных
3. Протестировать на реальных пользователях

**Для полного решения:**

1. Разработать Electron десктоп клиент
2. Интегрировать с существующим API
3. Добавить настройки приватности
4. Обеспечить безопасность данных

## 🛠️ Технические детали

### Библиотеки для Electron:

- `robotjs` - для глобальных хуков мыши/клавиатуры
- `active-win` - для получения активного окна
- `systeminformation` - для системной информации

### Библиотеки для браузера:

- Нативные Web APIs (`mousemove`, `keydown`, `visibilitychange`)
- `idle-js` - для отслеживания бездействия

## ❓ Ответ на вопрос

**Нужно ли добавлять в десктоп версию?**

**ДА, для полного отслеживания нужен десктоп клиент**, потому что:

- Браузер не может отслеживать активность вне окна
- Нужны системные разрешения для глобальных хуков
- Требуется доступ к списку приложений системы

**Но можно начать с браузерного отслеживания** для быстрого MVP и показа базовой активности.
