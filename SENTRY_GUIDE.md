# Руководство по работе с Sentry

## Вход в систему

1. Перейдите на [sentry.io](https://sentry.io)
2. Войдите в свою учетную запись
3. Выберите организацию **`automatonsoft`**
4. Откройте проект **`javascript-nextjs`**

## Основные разделы интерфейса

### 1. **Issues (Проблемы)** - Главная страница

**Что это:**

- Список всех ошибок, которые произошли в приложении
- Каждая ошибка группируется по типу и stack trace

**Как использовать:**

#### Просмотр ошибок:

- **Список Issues**: Все ошибки отсортированы по частоте (самые частые сверху)
- **Статус ошибки:**
  - 🔴 **Unresolved** - нерешенные ошибки
  - 🟡 **Resolved** - решенные ошибки
  - 🟢 **Ignored** - игнорируемые ошибки

#### Фильтрация:

- **По статусу**: Unresolved, Resolved, Ignored
- **По окружению**: Production, Development, Staging
- **По пользователю**: Фильтр по конкретному пользователю
- **По времени**: Last 24 hours, Last 7 days, Last 30 days
- **По поиску**: Поиск по тексту ошибки

#### Действия с ошибками:

- **Assign** - назначить ответственного разработчика
- **Ignore** - игнорировать ошибку (не будет показываться в списке)
- **Resolve** - пометить как решенную
- **Delete** - удалить ошибку

### 2. **Issue Details (Детали ошибки)**

**Что показывает:**

#### Основная информация:

- **Title**: Название ошибки
- **First Seen**: Когда ошибка впервые появилась
- **Last Seen**: Когда ошибка последний раз появлялась
- **Events**: Количество событий (сколько раз произошла)
- **Users Affected**: Сколько пользователей затронуто

#### Stack Trace:

- Полный стек вызовов, где произошла ошибка
- Файлы и номера строк
- Ссылки на исходный код (если настроены source maps)

#### Context:

- **User**: ID пользователя, email, username
- **Device**: Браузер, ОС, разрешение экрана
- **Tags**: Кастомные теги (например, `type: network_error`)
- **Breadcrumbs**: События, которые привели к ошибке:
  - Клики
  - Навигация
  - API запросы
  - Console logs
  - И т.д.

#### Session Replay:

- Видеозапись действий пользователя перед ошибкой
- Позволяет увидеть, что делал пользователь

#### Actions:

- **Resolve in Next Release**: Автоматически решить при следующем релизе
- **Ignore**: Игнорировать
- **Delete**: Удалить
- **Create Issue** (Jira/GitHub): Создать задачу в трекере

### 3. **Performance (Производительность)**

**Что показывает:**

- Время отклика API endpoints
- Медленные запросы
- Транзакции (отдельные операции)
- Время загрузки страниц

**Как использовать:**

- Найдите медленные эндпоинты
- Оптимизируйте запросы к БД
- Улучшайте время загрузки страниц

### 4. **Releases (Релизы)**

**Что это:**

- Версии вашего приложения
- Связь ошибок с конкретными версиями
- Source maps для разных версий

**Как использовать:**

- Создавайте релизы при деплое
- Отслеживайте, какие ошибки появились в новой версии
- Откатывайтесь к стабильным версиям

### 5. **Alerts (Уведомления)**

**Что это:**

- Настройка уведомлений о новых ошибках
- Email, Slack, Discord, PagerDuty и т.д.

**Как настроить:**

1. Перейдите в **Settings → Alerts**
2. Создайте правило уведомления:
   - При новой ошибке
   - При определенном количестве ошибок
   - При ошибке в определенном окружении
3. Выберите канал уведомлений

### 6. **Settings (Настройки)**

#### Project Settings:

- **General**: Название проекта, DSN
- **Client Keys (DSN)**: Ваш DSN для интеграции
- **Security**: Настройки безопасности
- **Filters**: Фильтры для игнорирования определенных ошибок

#### Alert Settings:

- Правила уведомлений
- Каналы уведомлений

#### Source Maps:

- Загрузка source maps для отображения исходного кода
- Настройка версий

## Типичные сценарии работы

### Сценарий 1: Новые ошибки в production

1. **Проверьте Issues** - откройте список нерешенных ошибок
2. **Отсортируйте по Last Seen** - начните с самых свежих
3. **Откройте детали ошибки**:
   - Изучите stack trace
   - Посмотрите Session Replay (если доступен)
   - Проверьте Breadcrumbs - что делал пользователь
   - Проверьте Context - какие данные были в момент ошибки
4. **Исправьте ошибку** в коде
5. **Resolve** ошибку в Sentry
6. **Создайте Release** с исправлением

### Сценарий 2: Мониторинг производительности

1. Перейдите в **Performance**
2. Посмотрите **Slowest Transactions**
3. Нажмите на медленный эндпоинт
4. Изучите:
   - Время выполнения
   - Количество запросов к БД
   - Время выполнения запросов
5. Оптимизируйте проблемные места

### Сценарий 3: Настройка уведомлений

1. Перейдите в **Settings → Alerts**
2. Создайте правило:
   ```
   Trigger: When a new issue is created
   Conditions:
     - Environment: production
     - Issue level: error
   Action: Send notification to Slack/Email
   ```
3. Сохраните правило

### Сценарий 4: Работа с тегами

**Теги** помогают группировать и фильтровать ошибки:

В нашем коде уже используются теги:

- `type: network_error` - для сетевых ошибок

**Как добавить свои теги:**

```typescript
Sentry.captureException(error, {
  tags: {
    feature: "time-tracking",
    component: "TimeTracker",
    userRole: "employee",
  },
});
```

**Как фильтровать по тегам:**

- В интерфейсе Issues используйте поиск: `tag:feature:time-tracking`
- Или выберите тег из списка тегов слева

### Сценарий 5: Игнорирование известных ошибок

Если ошибка известна и не критична:

1. Откройте Issue
2. Нажмите **Ignore**
3. Выберите условие:
   - **Forever** - игнорировать навсегда
   - **Until this affects more users** - игнорировать до определенного количества пользователей
   - **Until this happens more** - игнорировать до определенного количества событий

### Сценарий 6: Отслеживание исправлений

1. После исправления ошибки создайте **Release**:

   ```bash
   # В Sentry CLI или через API
   sentry-cli releases new <version>
   sentry-cli releases files <version> upload-sourcemaps ./dist
   ```

2. Отметьте Issue как **Resolved in Next Release**

3. После деплоя проверьте, что ошибка не повторяется

## Полезные советы

### 1. Используйте Breadcrumbs

Breadcrumbs показывают цепочку событий перед ошибкой. Это помогает понять контекст.

### 2. Session Replay

Включайте Session Replay для критичных ошибок - это как "черный ящик" для фронтенда.

### 3. Группируйте ошибки

Sentry автоматически группирует похожие ошибки. Используйте **fingerprinting** для кастомной группировки.

### 4. Отслеживайте тренды

Смотрите графики появления ошибок - это поможет найти паттерны.

### 5. Используйте Release Health

Отслеживайте здоровье релизов - процент успешных сессий, количество ошибок.

## Фильтры и поиск

### Поиск по тексту:

```
error:Network
message:"Failed to load"
```

### Фильтры:

- `is:unresolved` - только нерешенные
- `environment:production` - только production
- `user.email:user@example.com` - ошибки конкретного пользователя
- `level:error` - только ошибки уровня error
- `release:1.2.3` - ошибки в конкретном релизе

### Комбинированные фильтры:

```
is:unresolved environment:production level:error
```

## Интеграция с другими инструментами

### GitHub/GitLab:

- Создавайте Issues автоматически при новых ошибках
- Связывайте ошибки с PR и коммитами

### Slack/Discord:

- Получайте уведомления в реальном времени
- Настраивайте разные каналы для разных типов ошибок

### Jira:

- Создавайте задачи автоматически
- Связывайте ошибки с задачами

## Best Practices

1. **Регулярно проверяйте Issues** - не копите нерешенные ошибки
2. **Используйте теги** - для лучшей организации
3. **Настройте уведомления** - для критичных ошибок
4. **Создавайте Release** - при каждом деплое
5. **Используйте Source Maps** - для читаемых stack traces
6. **Игнорируйте шум** - настройте фильтры для известных неважных ошибок
7. **Мониторьте производительность** - не только ошибки

## Полезные ссылки

- [Документация Sentry](https://docs.sentry.io/)
- [Best Practices](https://docs.sentry.io/product/best-practices/)
- [Session Replay Guide](https://docs.sentry.io/platforms/javascript/session-replay/)
- [Performance Monitoring](https://docs.sentry.io/product/performance/)
